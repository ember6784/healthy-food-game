# 🎮 Смачна Гра - Деплой на Vercel

## 📦 Швидкий деплой

### Спосіб 1: Через Vercel Dashboard (Найпростіше)

1. **Переконайтесь, що проект збережено на GitHub:**
   ```bash
   git add .
   git commit -m "Ready for Vercel deployment"
   git push origin main
   ```

2. **Відкрийте [vercel.com](https://vercel.com)**

3. **Увійдіть через GitHub**

4. **Натисніть "Add New" → "Project"**

5. **Оберіть репозиторій `healthy-food-game`**

6. **Vercel автоматично налаштує все:**
   - Framework: Create React App ✓
   - Root Directory: ./
   - Build Command: `npm run build`
   - Output Directory: `build`

7. **Натисніть "Deploy"**

8. **Через 2-3 хвилини отримаєте URL!**

---

### Спосіб 2: Через Vercel CLI

```bash
# Перейдіть в папку проекту
cd C:\Users\User\healthy-food-game

# Встановіть Vercel CLI (якщо ще не встановлено)
npm install -g vercel

# Увійдіть в акаунт
vercel login

# Деплой
vercel --prod
```

---

## 📱 Створення QR-коду

Після успішного деплою:

1. **Скопіюйте URL вашого проекту** (наприклад: `https://healthy-food-game.vercel.app`)

2. **Відкрийте файл `qr-code.html`**

3. **Замініть URL на ваш:**
   ```javascript
   const GAME_URL = 'https://healthy-food-game.vercel.app'; // ← Ваш URL тут
   ```

4. **Відкрийте `qr-code.html` в браузері**

5. **Роздрукуйте сторінку** натиснувши кнопку "🖨️ Роздрукувати"

6. **Повісьте плакат в класі!** 🎉

---

## 🔧 Налаштування Vercel (якщо потрібно)

Створіть файл `vercel.json` в корені проекту:

```json
{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
```

---

## ✅ Перевірка після деплою

1. Відкрийте URL у браузері
2. Перевірте, що всі зображення завантажуються
3. Спробуйте зіграти одне завдання
4. Протестуйте на мобільному телефоні

---

## 🎯 Що далі?

- ✅ Поділіться посиланням з вчителями
- ✅ Роздрукуйте QR-код для класу
- ✅ Отримайте відгуки від дітей
- ✅ Додавайте нові продукти та сценарії за потребою

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте логи на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Переконайтесь, що всі файли закомічені в Git
3. Спробуйте `vercel --prod --force` для примусового деплою

---

**Успішного запуску! 🚀**
