# AGENTS.md - Довідник для AI агентів

> Цей файл містить технічну документацію про архітектуру та конвенції проекту для AI кодинг-агентів.

---

## 📋 Огляд проекту

**Назва:** Смачна Гра (healthy-food-game)  
**Призначення:** Освітня веб-гра для дітей 1-11 класів про здорове харчування  
**Основна мова:** Українська (інтерфейс, контент, документація)  
**Кодова база:** JavaScript ES6+, коментарі українською

Гра дозволяє дітям вчитися обирати здорову їжу через інтерактивні сценарії: збір сніданку, перекусу, обіду, а також спеціальні завдання (раціон спортсмена, контроль цукру, їжа для мозку).

---

## 🛠️ Технологічний стек

| Компонент | Технологія | Версія |
|-----------|-----------|--------|
| Фреймворк | React | 19.2.0 |
| Рендеринг | ReactDOM | 19.2.0 |
| Білд-інструмент | Create React App (react-scripts) | 5.0.1 |
| Стилізація | Tailwind CSS | 3.4.18 |
| CSS-постпроцесор | PostCSS | 8.5.6 |
| Автопрефіксер | Autoprefixer | 10.4.21 |
| Тестування | Jest + React Testing Library | вбудовано |
| Моніторинг | web-vitals | 2.1.4 |

---

## 📁 Структура проекту

```
healthy-food-game/
├── public/                    # Статичні файли
│   ├── index.html            # HTML-шаблон
│   ├── favicon.ico           # Іконка сайту
│   ├── logo192.png           # Логотипи PWA
│   ├── logo512.png
│   ├── manifest.json         # PWA маніфест
│   └── robots.txt            # Налаштування для пошукових роботів
├── src/                       # Джерельний код
│   ├── App.js                # Головний компонент гри (416 рядків)
│   ├── App.css               # Кастомні CSS-анімації
│   ├── App.test.js           # Тести (базовий шаблон)
│   ├── index.js              # Точка входу
│   ├── index.css             # Tailwind директиви + базові стилі
│   ├── logo.svg              # Логотип React
│   ├── reportWebVitals.js    # Моніторинг продуктивності
│   ├── setupTests.js         # Налаштування тестів
│   ├── components/           # (зарезервовано, порожньо)
│   ├── data/                 # Дані гри
│   │   ├── products.js       # База продуктів (1003 рядки)
│   │   └── scenarios.js      # Ігрові сценарії (249 рядків)
│   ├── hooks/                # (зарезервовано, порожньо)
│   └── utils/                # Утиліти
│       └── gameLogic.js      # Логіка оцінювання (455 рядків)
├── build/                     # Білд продакшену (генерується автоматично)
├── package.json              # Залежності та скрипти
├── package-lock.json         # Lock-файл залежностей
├── tailwind.config.js        # Конфігурація Tailwind
├── postcss.config.js         # Конфігурація PostCSS
├── vercel.json               # Налаштування деплою на Vercel
├── qr-code.html              # Генератор QR-кодів для друку
├── DEPLOYMENT.md             # Інструкція з деплою
└── README.md                 # Документація для користувачів
```

---

## 🚀 Команди для розробки

```bash
# Встановлення залежностей
npm install

# Запуск локального сервера розробки (http://localhost:3000)
npm start

# Збірка продакшен-версії
npm run build

# Запуск тестів у watch-режимі
npm test

# Видалення залежностей CRA (необоротно!)
npm run eject
```

---

## 🎮 Архітектура додатку

### Стан гри (state management)

Використовується React Hooks (`useState`), без Redux/Zustand:

```javascript
// Основні стани в App.js
const [gameState, setGameState] = useState('menu');        // 'menu' | 'playing' | 'results'
const [selectedAge, setSelectedAge] = useState(null);      // '1-5' | '6-11'
const [selectedScenario, setSelectedScenario] = useState(null);
const [selectedProducts, setSelectedProducts] = useState([]);
const [shuffledProducts, setShuffledProducts] = useState([]);
const [result, setResult] = useState(null);
```

### Потік гри

```
Меню → Вибір вікової групи → Вибір сценарію → Гра → Результати
```

### Основні функції App.js

| Функція | Призначення |
|---------|-------------|
| `startGame(ageGroup, scenario)` | Ініціалізація нової гри |
| `toggleProduct(product)` | Додавання/видалення продукту з корзини |
| `finishGame()` | Завершення гри та розрахунок балів |
| `backToMenu()` | Повернення до головного меню |
| `playAgain()` | Перезапуск поточного сценарію |
| `renderMenu()` | Рендер меню вибору |
| `renderGame()` | Рендер ігрового екрану |
| `renderResults()` | Рендер екрану результатів |

---

## 📊 База даних

### Продукти (`src/data/products.js`)

**Кількість:** 70+ продуктів  
**Категорії:** fruits, vegetables, dairy, grains, protein, fats, junk

Структура продукту:
```javascript
{
  id: 'apple',                    // Унікальний ідентифікатор
  name: 'Яблуко',                 // Назва українською
  category: 'fruits',             // Категорія
  healthScore: 90,                // Оцінка корисності (0-100)
  calories: 52,                   // Калорійність на 100г
  image: 'https://...',           // URL зображення (Unsplash/Pexels)
  description: '...',             // Короткий опис
  ageAppropriate: ['1-5', '6-11'] // Вікові групи
}
```

**Експортовані функції:**
- `shuffleArray(array)` - алгоритм Fisher-Yates
- `getProductsByCategory(category)` - фільтр за категорією
- `getProductById(id)` - пошук за ID
- `getProductsByAge(ageGroup)` - фільтр за віком
- `getShuffledProductsByAge(ageGroup)` - перемішані для гри

### Сценарії (`src/data/scenarios.js`)

**Кількість:** 8 сценаріїв  
- 1-5 класи: 3 сценарії (breakfast-1, snack-1, lunch-1)
- 6-11 класи: 5 сценаріїв (athlete-meal, daily-menu, sugar-control, brain-food, exam-prep)

Структура сценарію:
```javascript
{
  id: 'breakfast-1',
  title: 'Збери здоровий сніданок',
  description: '...',
  ageGroup: '1-5',                // '1-5' | '6-11'
  difficulty: 'easy',             // 'easy' | 'medium' | 'hard'
  goals: {
    minProducts: 5,
    maxProducts: 5,
    minHealthScore: 70,
    needsCategory: ['fruits', 'grains'],
    avoidCategories: ['junk'],
    maxCalories: 800,             // опціонально
    maxSugar: 30                  // опціонально
  },
  requiredProducts: 5,
  scoring: { /* правила нарахування */ },
  hint: '...'
}
```

---

## 🧮 Логіка оцінювання (`src/utils/gameLogic.js`)

### Основні функції

| Функція | Опис |
|---------|------|
| `calculateScore(products, scenario)` | Головна функція розрахунку балів |
| `getGrade(score, maxScore)` | Визначення оцінки (Відмінно/Добре/...) |
| `getHint(products, scenario)` | Генерація підказок під час гри |
| `formatResult(result)` | Форматування для відображення |

### Система нарахування балів

1. **Кількість продуктів** (20%) - відповідність min/max
2. **Корисність продуктів** (40%) - середній healthScore
3. **Відсутність шкідливих** (20%) - без категорії junk
4. **Необхідні категорії** (20%) - наявність needsCategory
5. **Бонуси:** різноманітність (+5), штраф за дисбаланс категорій

### Оцінки

| Діапазон | Оцінка | Emoji | Зірки |
|----------|--------|-------|-------|
| 90-100% | Відмінно! | 🌟 | 5 |
| 75-89% | Добре! | 😊 | 4 |
| 60-74% | Непогано | 🤔 | 3 |
| 40-59% | Можна краще | 😐 | 2 |
| <40% | Спробуй ще | 😕 | 1 |

---

## 🎨 Стилізація

### Tailwind CSS

Конфігурація: `tailwind.config.js` - стандартна, без кастомізацій.

### Кастомні CSS (`App.css`)

**Анімації:**
- `animate-gradient` - анімований градієнт фону
- `animate-bounce-slow` - повільне підстрибування
- `animate-slide-up` - поява знизу
- `animate-scale-in` - масштабування
- `animate-star` - анімація зірок

**Кастомні класи:**
- `.game-title` - тінь для заголовку
- `.scenario-card` - анімація карток сценаріїв
- `.product-card` - ефекти для карток продуктів

### Кольорова схема

- Фон меню: `from-purple-400 via-pink-400 to-red-400`
- Фон гри: `from-blue-200 via-purple-200 to-pink-200`
- Фон результатів: `from-yellow-300 via-orange-300 to-red-300`
- Категорії продуктів мають власні кольори в `categories` об'єкті

---

## 📝 Конвенції коду

### Мова

- **Всі коментарі та докстрінги:** українською
- **Код (змінні, функції):** camelCase англійською
- **Користувацький інтерфейс:** українською
- **Константи:** UPPER_SNAKE_CASE

### Стиль коду

```javascript
// ✅ Правильно
/**
 * Опис функції українською
 * @param {Array} products - масив продуктів
 * @returns {number} розрахований бал
 */
const calculateScore = (products, scenario) => {
  // логіка
};

// ✅ Правильно - зрозучі назви
const selectedProducts = [];
const isGameActive = true;

// ❌ Неправильно
const sp = [];  // незрозуміла абревіатура
const game = true;  // недостатньо конкретно
```

### Організація файлів

- Дані відокремлені від логіки (`data/` vs `utils/`)
- Компоненти React знаходяться в корені `src/` (не в `components/`)
- Кожен модуль має докстрінг з описом

---

## 🧪 Тестування

### Налаштування

- Jest (вбудований в CRA)
- React Testing Library
- jest-dom для додаткових матчерів

### Поточні тести

Файл `src/App.test.js` містить базовий шаблонний тест (не актуальний).

### Запуск тестів

```bash
npm test
```

Запускається в інтерактивному watch-режимі.

---

## 🚀 Деплой

### Підтримувані платформи

1. **Vercel** (рекомендовано) - налаштування в `vercel.json`
2. **Netlify** - підтримується з `build/` директорією

### Конфігурація Vercel (`vercel.json`)

```json
{
  "version": 2,
  "builds": [{ "src": "package.json", "use": "@vercel/static-build" }],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
```

### Процес деплою

1. Збілдити: `npm run build`
2. Закомітити: `git add build && git commit`
3. Пуш на GitHub
4. Підключити репозиторій в Vercel Dashboard
5. Vercel автоматично визначить Create React App

---

## 🔄 Робота з даними

### Додавання нового продукту

```javascript
// src/data/products.js
{
  id: 'unique-id',
  name: 'Назва українською',
  category: 'fruits',  // одна з існуючих категорій
  healthScore: 85,     // 0-100
  calories: 100,       // на 100г
  image: 'https://images.unsplash.com/...',  // або Pexels
  description: 'Короткий опис українською',
  ageAppropriate: ['1-5', '6-11']  // або ['6-11'] для складних
}
```

### Додавання нового сценарію

```javascript
// src/data/scenarios.js
{
  id: 'unique-id',
  title: 'Назва завдання',
  description: 'Опис',
  ageGroup: '6-11',
  difficulty: 'medium',  // easy | medium | hard
  goals: {
    minProducts: 5,
    maxProducts: 7,
    minHealthScore: 70,
    needsCategory: ['fruits', 'vegetables'],
    avoidCategories: ['junk'],
    maxCalories: 600  // опціонально
  },
  requiredProducts: 6,
  scoring: {
    perfectScore: 100,
    healthyProduct: 15,
    unhealthyProduct: -10,
    balancedDiet: 20
  },
  hint: 'Підказка для гравця'
}
```

---

## 🔒 Безпека

- Немає серверної частини - тільки клієнтський код
- Немає збору персональних даних
- Немає API ключів або секретів
- Зображення завантажуються з зовнішніх CDN (Unsplash, Pexels)

---

## 🐛 Відомі особливості

1. **Порожні директорії:** `src/components/` та `src/hooks/` створені для майбутнього розширення, але наразі порожні

2. **Тести:** Не реалізовані повноцінно (лише шаблон CRA)

3. **Зображення:** Використовуються зовнішні CDN - гра вимагає інтернет-з'єднання

4. **Зберігання прогресу:** Не реалізовано (LocalStorage не використовується)

5. **PWA:** Маніфест присутній, але сервіс-воркер не налаштований для офлайн-режиму

---

## 📚 Корисні посилання

- [Create React App документація](https://create-react-app.dev/)
- [Tailwind CSS документація](https://tailwindcss.com/)
- [React документація](https://react.dev/)
- [Vercel деплой](https://vercel.com/docs)

---

## 🤝 Рекомендації щодо розробки

1. **Не переіменовувати функції** без причини
2. **Розширювати існуючі функції** замість створення нових
3. **Зберігати єдиний стиль коду** - див. приклади вище
4. **Додавати докстрінги** до нових функцій українською
5. **Мінімум дублювання коду** - використовувати утиліти з `gameLogic.js`
6. **Тестувати на мобільних** - гра орієнтована на планшети/телефони
